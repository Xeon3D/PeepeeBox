* Updating from inside PeepeeBox works again. Taking 1.11 stopped partway with "inflate failed (-5)": the updater's unzipper gave up at a point in the archive where it only had to read on, and 1.11's archive had one. It failed before touching anything, so the folder was left as it was.
* 1.10 and 1.11 still have the old unzipper, so every release archive is now checked against it before it is published, and repacked if it would trip it. This one was, so 1.10 and 1.11 update to 1.12 by themselves — including a copy that already failed on 1.11: **Preferences > Updates > Check now**, then Update.
